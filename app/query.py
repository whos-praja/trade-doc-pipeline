"""Query agent (STUB).

Planned role: answer natural-language questions over the stored extractions
(e.g. "show all shipments to Rotterdam on CIF terms", "which invoices are
missing an HS code?") by translating them into queries over the SQLite store.
"""
from __future__ import annotations


def answer(question: str) -> str:
    """Answer a natural-language question over stored extractions. Not implemented yet."""
    raise NotImplementedError("Query agent is a POC stub.")
