# Trade-document pipeline POC — Python 3.11+
anthropic>=0.40          # Claude provider (default; LLM_PROVIDER=anthropic)
google-genai>=1.75.0     # optional Gemini provider; current SDK, not the deprecated google-generativeai
pydantic>=2.7
pymupdf>=1.24
python-dotenv>=1.0
rapidfuzz>=3.0           # deterministic fuzzy matching in the Validator (difflib fallback)
streamlit>=1.40          # one-screen UI (ui/streamlit_app.py)
# SQLite is part of the Python standard library (no dependency needed).
